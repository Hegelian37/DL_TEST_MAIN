#ifndef HEADER_H
#define HEADER_H

#include <iostream>
#include <vector>
#include <sstream>
#include <string>
#include <cctype>

using namespace std;

bool isValidNumber(const string& str);
string preprocessInput(const string& input);

#endif
